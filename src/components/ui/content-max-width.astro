---
import { twMerge, type ClassNameValue } from "tailwind-merge";

interface Props {
  className?: ClassNameValue;
}
const { className } = Astro.props;
---

<article
  class={`${twMerge(
    "w-full flex flex-col gap-5 py-4 md:w-[750px]",
    className,
  )}`}
>
  <slot />
</article>
